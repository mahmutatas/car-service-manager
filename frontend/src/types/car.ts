export interface Car {
  id: number;
  licensePlate: string;
  model: string;
  brand: string;
}

export interface CarRequest {
  licensePlate: string;
  model: string;
  brand: string;
}export interface Car {
  id: number;
  licensePlate: string;
  model: string;
  brand: string;
}

export interface CarRequest {
  licensePlate: string;
  model: string;
  brand: string;
}